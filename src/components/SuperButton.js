import React, { Component } from "react";
import styles from './SuperButton.module.css'

export default class SuperButton extends Component {
    render() {
        return (<div class="heading"><button>SuperButton</button></div>)
    }
}